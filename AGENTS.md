# Repository instructions

When asked to compare benchmark results in one `docs/<BenchmarkName>/` directory, use the project skill at [`skills/compare-benchmark-results/SKILL.md`](skills/compare-benchmark-results/SKILL.md).

